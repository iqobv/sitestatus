import { SkeletonLoader } from '@/components/ui';

const MonitorIncidentsLoader = () => {
	return <SkeletonLoader width="100%" height={200} borderRadius={8} />;
};

export default MonitorIncidentsLoader;
