'use client';

import { SkeletonLoader } from '@/components/ui';

const MonitorHeatmapLoader = () => {
	return <SkeletonLoader height={400} borderRadius={8} width="100%" />;
};

export default MonitorHeatmapLoader;
